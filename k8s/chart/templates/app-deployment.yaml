apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -c
    kompose.version: 1.34.0 (HEAD)
  labels:
    io.kompose.service: client
  name: client
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: client
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -c
        kompose.version: 1.34.0 (HEAD)
      labels:
        io.kompose.service: client
    spec:
      imagePullSecrets:
        - name: registry-1
      containers:
        - env:
            {{- range $key, $value := index .Values "client" "env" }}
              {{- if kindIs "map" $value }}
              - name: {{ $key }}
                valueFrom:
                  secretKeyRef:
                    name: {{ $value.secretKeyRef.name }}
                    key: {{ $value.secretKeyRef.key }}
              {{- else }}
              - name: {{ $key }}
                value: {{ $value | quote }}
              {{- end }}
              {{- end }}
          image: "{{ .Values.client.image.repository }}:{{ .Values.client.image.tag }}"
          name: client
#          resources:
#            limits:
#              cpu: 200m
#              memory: 256Mi
#            requests:
#              cpu: 200m
#              memory: 128Mi
          ports:
            - containerPort: 80
              protocol: TCP
      restartPolicy: Always